import { ClientsSortPipe } from './clients-sort.pipe';

describe('ClientsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
