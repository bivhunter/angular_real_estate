import { HomesSortPipe } from './homes-sort.pipe';

describe('HomesSortPipe', () => {
  it('create an instance', () => {
    const pipe = new HomesSortPipe();
    expect(pipe).toBeTruthy();
  });
});
