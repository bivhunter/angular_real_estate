import { ToogleValidatoreDirective } from './toogle-validatore.directive';

describe('ToogleValidatoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleValidatoreDirective();
    expect(directive).toBeTruthy();
  });
});
